<div class="header mt-3">
  <h1>COLABORADORES</h1>
  <div class="d-flex justify-content-start gap-2">
    <%= link_to new_collaborator_path, class: "btn btn-primary", data: { turbo_frame: dom_id(User.new) } do %>
      <i class="bi bi-plus-circle-fill"></i> Nuevo colaborador
    <% end %>
  </div>
</div>

<div class="overflow-x-scroll">
  <div class="d-flex border rounded px-3 mt-2 align-items-center bg-success text-white" style="min-width: 1200px;">
    <div class="col"><small>NOMBRE</small></div>
    <div class="col"><small>CORREO ELECTRÓNICO</small></div>
    <div class="col"><small>ROL</small></div>
    <div class="col"><small>ACCIONES</small></div>
  </div>
  <%= turbo_frame_tag User.new %>
  <%= turbo_frame_tag "collaborators" do %>
    <%= render partial: "collaborators/collaborator", collection: @collaborators %>
  <% end %>
</div>